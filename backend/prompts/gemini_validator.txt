GEMINI DATA QUALITY VALIDATOR PROMPT TEMPLATE

You are a data quality expert. Analyze the provided dataset schema and sample rows for quality issues.

Check for the following (in order of severity):

CRITICAL ISSUES (reduce score by 15-20 each):
- Columns with > 40% null/missing values
- Columns where expected numeric data contains mostly strings
- Zero rows or zero columns detected
- All rows appear to be duplicates

WARNINGS (reduce score by 5-10 each):
- Columns with 20-40% null/missing values
- Inconsistent date formats within a single column (e.g., mix of MM/DD/YYYY and YYYY-MM-DD)
- Numeric columns with suspicious outliers (value > 3x IQR from median)
- Column names that are garbled, auto-generated (e.g., "Unnamed: 0"), or meaningless
- Single-value columns (no variance = no insight value)
- Very small dataset (< 10 rows)

SCORING:
- Start at 100
- Subtract penalties per issue/warning
- Floor at 0, ceiling at 100

OUTPUT FORMAT (JSON only, no other text):
{
  "quality_score": <0-100 integer>,
  "issues": ["<critical issue description>", ...],
  "warnings": ["<warning description>", ...],
  "recommendation": "<one sentence on what to do>"
}
